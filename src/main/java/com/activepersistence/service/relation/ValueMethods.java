package com.activepersistence.service.relation;

public enum ValueMethods {
   INCLUDES, EAGER_LOAD, SELECT, GROUP, ORDER, JOINS, UNSCOPE, BIND,
   LIMIT, OFFSET, LOCK, DISTINCT, CONSTRUCTOR,
   WHERE, HAVING, FROM
}
