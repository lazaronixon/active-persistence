package com.activepersistence.service.relation;

public enum ValueMethods {
   INCLUDES, EAGER_LOAD, SELECT, GROUP, ORDER, JOINS, UNSCOPE,
   LIMIT, OFFSET, LOCK, DISTINCT, REORDERING, CONSTRUCTOR,
   WHERE, HAVING, FROM
}
