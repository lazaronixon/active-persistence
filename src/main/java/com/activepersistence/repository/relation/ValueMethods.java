package com.activepersistence.repository.relation;

public enum ValueMethods {
   INCLUDES, EAGER_LOAD, SELECT, GROUP, ORDER, JOINS, UNSCOPE,
   LIMIT, OFFSET, LOCK, DISTINCT, REORDERING, CONSTRUCTOR, READONLY,
   WHERE, HAVING, FROM
}
